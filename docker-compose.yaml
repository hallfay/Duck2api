version: '3.8'

services:
  duck-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5179:5179"
    volumes:
      - ./duck.py:/app/duck.py
    restart: unless-stopped
